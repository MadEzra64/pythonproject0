# Declares how many types of people there are
types_of_people = 10

# Declaring that x equals a string containing a variable.
x = f"There are {types_of_people} types of people."

# Declaring the variable named binary equals the string binary
binary = "binary"

# Declaring the variable do_not to equal the string don't
do_not = "don't"

# Declaring the variable y is equal to a string with multiple variables.
y = f"Those who know {binary} and those who {do_not}."

# Printing the variables x and y
print(x)
print(y)

# Printing the variables x and y again within a formated string.
print(f"I said: {x}")
print(f"I also said: '{y}'")

# Declaring the variable named hilarious is a False boolean value
hilarious = False
# Declaring joke_evaluation is equal to a string of text with an empty variable.
joke_evaluation = "Isn't that joke so funny?! {}"

# Printing both string variables together with the print function format.
print(joke_evaluation.format(hilarious))

# Declaring what w and e equal
w = "This is the left side of..."
e = "a string with a right side."

# Printing w and e together with an operation
print(w + e)

# Extra line of code to test GitHub remote host
